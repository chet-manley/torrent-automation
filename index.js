#!/bin/env node
'use strict'

/*
const processType = process.env.PROCESS_TYPE || 'main'
let child

try {
  child = require(`./workers/${processType}`)
} catch (ex) {
  if (ex.code === 'MODULE_NOT_FOUND') {
    throw new Error(`No worker for: ${processType}`)
  }
  throw ex
}
*/

//module.exports = child
